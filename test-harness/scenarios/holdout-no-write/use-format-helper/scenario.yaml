# Holdout should-not-capture (§4.4): routine refactor on the bigger-app
# fixture, no durable insight. Any create/update is a false positive.
id: holdout-no-write/use-format-helper
version: 1
class: should-not-capture
fixture: fixtures/bigger-app
task: 'The invoice list route formats amounts inline even though formatAmount() exists. Use the helper instead of duplicating the expression.'
corpus: corpus/standard-15
checks:
  task_success: 'bun run check'
capture_rubric: null
