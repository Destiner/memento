# Codex Phase 2, block B: the crowded env at N=10. On claude-code, crowding
# TRIGGERED usage (deferred-tool search surfaced memento at the moment of need);
# whether gpt-5.6-sol shows the same inversion is unknown and decides how the
# recommended install behaves in real many-MCP setups.
# Rep-count cap: 480 reps × $2 × 1.2.
name: codex-p2-crowded
harness: codex
model: gpt-5.6-sol
env: crowded
reps: 10
concurrency: 3
max_spend_usd: 1160
configs: [baseline-0, claude-md-trigger, shipped-v2, shipped-v2-agents]
scenarios: [read/*, no-read/*, write/*, no-write/*]
