# One-off fill for the calibration run: its final cell (no-write/extract-url-helper
# rep 2) was lost when the runner hung awaiting a session with no live child
# process (suspected runSession timeout/close race — see session.ts). Reruns that
# scenario only; analysis unions run=calibration + run=calibration-fill.
name: calibration-fill
model: claude-opus-4-8
env: clean
reps: 2
concurrency: 2
max_spend_usd: 5
configs: [baseline-no-memento]
scenarios: [no-write/extract-url-helper]
