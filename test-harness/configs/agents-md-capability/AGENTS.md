# Project notes

## Memory (memento)

Memento is installed. Use the memento `search_memory` and `read_memory` tools
whenever you need read access to prior knowledge, and `create_memory` when you
need write access to it — rather than assuming no memory is available. Treat
code and current repository docs as more authoritative than memory.
