# agents-md-decision: phrasing-iteration arm (decision-check framing). Reframes
# the task-shape finding as an instruction: ask whether a prior decision covers
# the work, without enumerating example domains (kept generic to avoid
# overfitting to test scenarios). Same channel and floor as claude-md-trigger.
name: agents-md-decision
description: Project fragment, decision-check framing — plain variant, both sides.
knob: claude-md
variant: decision-check
side: both
portability: partial
memento_variant: plain
install:
  claude_md: AGENTS.md
